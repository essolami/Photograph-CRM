ALTER TABLE "Client" ALTER COLUMN "email" DROP NOT NULL,
ADD COLUMN "defenseDate" DATE,
ADD COLUMN "facultyId" INTEGER,
ADD COLUMN "packId" INTEGER,
ADD COLUMN "isDuo" BOOLEAN NOT NULL DEFAULT false,
ADD COLUMN "facultyName" TEXT,
ADD COLUMN "packName" TEXT,
ADD COLUMN "basePrice" DECIMAL(10,2) NOT NULL DEFAULT 0,
ADD COLUMN "supplements" JSONB NOT NULL DEFAULT '[]',
ADD COLUMN "discount" DECIMAL(10,2) NOT NULL DEFAULT 0,
ADD COLUMN "total" DECIMAL(10,2) NOT NULL DEFAULT 0,
ADD COLUMN "photographerId" INTEGER REFERENCES "Photographer"("id") ON DELETE SET NULL,
ADD COLUMN "editorId" INTEGER REFERENCES "Editor"("id") ON DELETE SET NULL,
ADD COLUMN "status" TEXT NOT NULL DEFAULT 'En cours',
ADD COLUMN "driveUrl" TEXT,
ADD COLUMN "comment" TEXT,
ADD COLUMN "grossProfit" DECIMAL(10,2);
